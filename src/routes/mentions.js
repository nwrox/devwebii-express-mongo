// import { controllers } from '../controllers'
//
// export const mentions = (express, mongoose) => {
//   const { createMention, listMentions } = controllers(mongoose)
//   const r = express.Router()
//
//   r.get('/', listMentions)
//   r.post('/', createMention)
//
//   return r
// }
